const sv = "http://animewebsite.hopto.org"
const config = {
    VideoServer: `${sv}:4000`,
    EntityServer: `${sv}:8000`,
    ImageServer: `${sv}:8080`,
    RoomServer: `${sv}:5000`,
    ChatServer: `ws://animewebsite.hopto.org:5000/ws`
}

export default config